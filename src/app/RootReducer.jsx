const initialState = {
    slides: [],
    ui: {
        tutorial: false,
        icons: false,
        menu: false,
        code: false,
        fullscreen: false,
    }
}

export default function RootReducer (state = initialState, action) {

    return state;

}